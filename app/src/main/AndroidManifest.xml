<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/african_shipping"
        android:label="@string/app_name"
        android:roundIcon="@drawable/african_shipping"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidProject"
        tools:targetApi="31">


        <activity
            android:name=".ui.services.view_goods"
            android:exported="false" />
        <activity
            android:name=".ui.services.add_to_cart"
            android:exported="false" />
        <activity
            android:name=".ui.admin_panel.insertion_admin"
            android:exported="false" />
        <activity
            android:name=".ui.services.shipping"
            android:exported="false" />

        <activity
            android:name=".splash_screen"
            android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AndroidProject.NoActionBar"/>


    </application>

</manifest>